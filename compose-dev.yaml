services:
  app:
    image: ubuntu:latest
    command: 
      - bin/sh
      - -c
      | apt update
      | apt intall python3 -y
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock
